spring:
  application:
    name: ai-msp-intelligence

  datasource:
    url: ${DB_URL:jdbc:h2:file:./data/aimspdb;AUTO_SERVER=TRUE}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
    driver-class-name: org.h2.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 20

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: always
      data-locations: classpath:data.sql

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Asia/Seoul

server:
  port: 8080

app:
  gemini:
    api-key: ${GEMINI_API_KEY:}
    api-url: https://generativelanguage.googleapis.com/v1beta/models
    model: gemini-3.1-flash-lite-preview
    max-tokens: 4096
    rate-limit-ms: 15000  # gemini-2.5-flash-lite free tier: 10 RPM → 15초 간격 (충분한 버퍼)
  naver:
    client-id: ${NAVER_CLIENT_ID:}
    client-secret: ${NAVER_CLIENT_SECRET:}
  procurement:
    api-key: ${PROCUREMENT_API_KEY:}  # 조달청 나라장터 공공데이터 API 키 (data.go.kr)
  saramin:
    api-key: ${SARAMIN_API_KEY:}      # 사람인 Open API 키 (oapi.saramin.co.kr)
  crawl:
    request-delay-ms: 2000

logging:
  level:
    com.aimsp: INFO
    org.springframework.web: WARN

---
spring:
  config:
    activate:
      on-profile: prod-debug

logging:
  level:
    com.zaxxer.hikari: DEBUG
    org.hibernate.engine.jdbc.env: DEBUG
    org.springframework.boot.autoconfigure.jdbc: DEBUG

---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: validate
    defer-datasource-initialization: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_batch_fetch_size: 20

  sql:
    init:
      mode: never
      data-locations: classpath:data-prod.sql

  h2:
    console:
      enabled: false
