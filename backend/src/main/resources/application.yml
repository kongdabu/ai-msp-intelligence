spring:
  application:
    name: ai-msp-intelligence

  datasource:
    url: ${DB_URL:jdbc:h2:file:./data/aimspdb;AUTO_SERVER=TRUE}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  sql:
    init:
      mode: always
      data-locations: classpath:data.sql

  flyway:
    enabled: false  # H2 개발 환경에서는 Flyway 비활성화

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: Asia/Seoul

server:
  port: 8080

app:
  gemini:
    api-key: ${GEMINI_API_KEY:}
    api-url: https://generativelanguage.googleapis.com/v1beta/models
    model: gemini-2.5-flash
    max-tokens: 4096
  crawl:
    interval-hours: ${CRAWL_INTERVAL_HOURS:24}
    request-delay-ms: 2000
  insight:
    cron: ${INSIGHT_CRON:0 0 7 * * ?}

logging:
  level:
    com.aimsp: INFO
    org.springframework.web: WARN

---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always
      data-locations: classpath:data-prod.sql

  flyway:
    enabled: true
    baseline-on-migrate: true   # 기존 DB에 flyway_schema_history 없어도 진행
    baseline-version: 1          # V1은 기존 스키마(baseline), V2부터 실행
    locations: classpath:db/migration

  h2:
    console:
      enabled: false
